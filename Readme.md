## Backend

### 📂 Folder: `server`

**Technologies:**
- Node.js + TypeScript
- Express
- OpenAI API
- CORS

### ⚡ Key features:
- AI transaction categorization
- Anomaly & High Alert detection
- Personalized financial insights generation

### 🛠 API Route

**POST** `/api/insights`

- **Description:**
Analyzes a list of transactions, categorizes them, highlights anomalies and generates financial insights using AI.

- **Request Body:**

```json
{ 
"demoTransactions": [ 
{ "id": 1, "description": "Uber trip Kyiv", "amount": -320 }, 
{ "id": 2, "description": "McDonald's", "amount": -189 } 
]
}
```
### Response:
```json
{ 
"transactions": [ 
{ 
"id": 1, 
"description": "Uber trip Kyiv", 
"amount": -320, 
"category": "Transport", 
"anomaly": true, 
"alertReason": "Transport payment unusually high 🔥" 
}, 
{ 
"id": 2, 
"description": "McDonald's", 
"amount": -189, 
"category": "Food" 
} 
], 
"insights": [ 
"You spent unusually high on Transport 🚗", 
"Multiple subscriptions detected 📺", 
"Consider reviewing large payments 🔥"
]
}
```
## Notes:

> transactions — original transactions with added categories and anomaly tags.

> anomaly — true if AI detected a suspicious transaction.

> alertReason — reason for High Alert (if any).

> insights — 3–5 short financial recommendations generated by AI.

### 🚀 Launch:

```bash
git clone https://github.com/guboss3214/Finance-AI-Server.git
cd Finance-AI-Server
npm install
npm run dev
```
### ⚙️ Environment variables
- Create a .env file in the server folder with the following content:

```bash
PORT=5000
CLIENT_URL=http://localhost:5173/
OPENAI_API_KEY=your_openai_api_key_here
PORT — the port on which the backend is running (default 5000)
```
- CLIENT_URL — the frontend URL for CORS

- OPENAI_API_KEY — your OpenAI key for accessing GPT-5 Mini